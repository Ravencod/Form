                                                            <!-- FORM type form_for -->

<!--<html>
  <body>
  <div class="container">
    <%= form_for :user do |f| %>
    <div class="form-group">
      Username: <%= f.text_field :username, class: 'form-control', autofocus: true %>
      </div>

      <div class="form-group">
      Email: <%= f.text_field :email, class: 'form-control', autofocus: true %>
      </div>

      <div class="form-group">
      Bio : <%= f.text_area :bio, class: 'form-control', autofocus: true%>
      </div>
      <%= f.submit "Submit", class: "btn btn-primary"  %>
  </div>
  </body>
</html>
<% end %>-->

                                                            <!-- FORM type form_tag -->
<!--<html>
  <body>
  <div class="container">
    <%= form_tag url_for(action: 'create'), method: "post" do %>

      <div class="form-group">
      <%= label_tag 'username', 'Username'%>
      <%= text_field_tag :username, params[:username], class: 'form-control', autofocus: true %>
      </div>

    <div class="form-group">
      <%= label_tag 'email', 'Email' %>
      <%= text_field_tag :email, params[:email], class: 'form-control', autofocus: true %>
    </div>

    <div class="form-group">
      <%= label_tag 'Bio' %>
      <%= text_field_tag :bio, params[:bio], class: 'form-control', autofocus: true %>
    </div>

    <%= submit_tag "Submit", class: "btn btn-primary" %>
    <% end %>
  </body>
</html>-->



                                                        <!-- FORM type base form  -->
<html>
  <body>
    <div class="container">
      <form action="/users/new" method="POST">
      <input type="hidden" name="authenticity_token" value="<%= form_authenticity_token %>">
        <div class="form-group">
            <label for="exampleInputPassword1">Username</label>
            <input type="text" class="form-control" placeholder="Enter a new username" name="username" autofocus: true>
        </div>
        <div class="form-group">
          <label for="exampleInputEmail1">Email address</label>
          <input type="text" class="form-control" id="exampleInputEmail1" aria-describedby="emailHelp" placeholder="Enter email" name="email">
          <small id="emailHelp" class="form-text text-muted">We'll never share your email with anyone else.</small>
        </div>
        <div class="form-group">
            <label for="exampleInputPassword1">Bio</label>
            <input type="text" class="form-control" placeholder="Write here ur bio" name="bio">
        </div>
        <button type="submit" class="btn btn-primary">Submit</button>
      </form>
    </div>
  </body>
</html>
